# =============================================================================
# Digital Twin Career Engine — зависимости
# =============================================================================
#
# Установка обязательного ядра:
#   pip install -r requirements.txt
#
# Установка конкретной группы опциональных либ:
#   pip install anthropic                  # если LLM_PROVIDER=anthropic
#   pip install pdfplumber                 # если нужен PDF-парсер
#   pip install telethon                   # если нужен парсинг Telegram-каналов
# =============================================================================

# -----------------------------------------------------------------------------
# CORE — всегда обязательно
# -----------------------------------------------------------------------------
pandas>=2.0.0
streamlit>=1.35.0
python-dotenv>=1.0.0
numpy>=1.26.0
matplotlib>=3.7.0
requests>=2.31.0
beautifulsoup4>=4.12.0

# ML — TF-IDF семантический матчинг (model/semantic_matcher.py)
scikit-learn>=1.4.0

# Testing
pytest>=8.0.0

# -----------------------------------------------------------------------------
# GenAI / LLM — раскомментировать ОДИН провайдер
# Задать в .env: LLM_PROVIDER=anthropic | openai | gemini
# -----------------------------------------------------------------------------
# anthropic>=0.28.0          # LLM_PROVIDER=anthropic  (Claude)
# openai>=1.30.0             # LLM_PROVIDER=openai     (GPT-4o и др.)
# google-generativeai>=0.7.0 # LLM_PROVIDER=gemini     (Gemini 1.5 Flash/Pro)

# -----------------------------------------------------------------------------
# PDF — парсер резюме (utils/pdf_parser.py)
# Раскомментировать ОДНУ библиотеку (рекомендуется pdfplumber)
# -----------------------------------------------------------------------------
# pdfplumber>=0.10.0     # рекомендуется: точный парсинг текста и таблиц
# pypdf>=4.0.0           # fallback: лёгкий, без зависимостей
# pdfminer.six>=20221105 # fallback: низкоуровневый, максимальная совместимость

# -----------------------------------------------------------------------------
# Telegram — парсинг каналов с вакансиями (telegram_bot/job_scraper.py)
# Нужен: API_ID + API_HASH → https://my.telegram.org/apps
# -----------------------------------------------------------------------------
# telethon>=1.36.0       # парсинг каналов (@dev_kg, @python_jobs и др.)

# -----------------------------------------------------------------------------
# Telegram Bot — опционально, НЕ нужен для базового функционала
# -----------------------------------------------------------------------------
# python-telegram-bot>=21.0  # только если нужен собственный Telegram-бот
