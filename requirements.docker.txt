# =============================================================================
# requirements.docker.txt — ВСЕ зависимости для Docker-образа
# (включая опциональные; используется только Dockerfile)
# Для локальной установки используй requirements.txt
# =============================================================================

# --- Core ---
pandas>=2.0.0
streamlit>=1.35.0
python-dotenv>=1.0.0
numpy>=1.26.0
matplotlib>=3.7.0
scikit-learn>=1.4.0
pytest>=8.0.0

# --- GenAI / LLM (все провайдеры — нужный выбирается через LLM_PROVIDER в .env) ---
anthropic>=0.28.0
openai>=1.30.0
google-generativeai>=0.7.0

# --- PDF парсер резюме ---
pdfplumber>=0.10.0
pypdf>=4.0.0

# --- Telegram — парсинг каналов с вакансиями ---
telethon>=1.36.0
