# Скопируй этот файл в .env и заполни ключи (если нужен LLM-режим)

# Провайдер: "anthropic" или "openai"
LLM_PROVIDER=anthropic

# API-ключ (опционально — без него агент работает в fallback-режиме)
LLM_API_KEY=

# GitHub токен (опционально — для GitHub Search API)
GITHUB_TOKEN=
